Implement a memory tool.

When the user tells a specific future behavior, create a short summary about it and load it with the initial context prompt.

create a sub-folder called "memory" and store the memory there, as separate .md files, inside the chat-history folder! (pull from settings)

The memory should be loaded with the initial context prompt.
