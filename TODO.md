# BUGZ

- when pressing the "Start convesation" through the quick launch, it start even if the state is on. 
- sometimes it starts two voice convo on error.

tools:
    - search still lists Registry, make that better

# Mobile
- on mobile when focus is not on, it drops connection.
    - sadly, this can't be fixed. Need a standalone app.


# TELEMETRY
- append posthog for telemetry data
- post context like:
    - how many files in repo
    - how many folders in repo
    - tool call telemetry average

- add telemetry to tool call functions like search, context, etc




# FEATURES
- add version info to settings
- add more ellaborate settings and concept description

- add/edit tools:
    - insert line
        - handle line numbers, find out how to do that best.
    - insert block
    - toggle todo
    - add user behavior tracking - serialize actions the user does in Obsidian
    - fast_read_multiple_files (into the same context)
    - web_search
        - replace search engine for web tool, google takes 18 secs occasionally
    - save_with_references
        - create a tool, that automatically always does the referencing:
            - local: looking at relevant links in vault
            - web: looking at relevant links in web - youtube, books, etc.



# SETTINGS
- settings page
    - make all tool calls configurable
- add optional sounds to toolcalls
    - particularly for the slow ones


# IDEA
- create a tool registry, where users can just add their own tools with AI!
- auto download book from libgen

- look up 

# RESEARCH
- notification are when assistant is active
    - make it toggleable in settings, but default to have it, the first time it's switched on.
    - can I make a hotkey to directly that?



    